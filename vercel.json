{
    "version": 2,
    "buildCommand": "npm run build",
    "outputDirectory": "public",
    "framework": "vite",
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/"
        }
    ]
}
